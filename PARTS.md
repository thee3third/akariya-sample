# PARTS.md — 部品カタログ

このサイトは「**色変数・文言・写真の差し替えだけで、別の居酒屋の実案件が8割完成する**」ことをゴールに設計しています。
再利用前提のセクションには HTML 内に `<!-- @parts: 名前 -->` 〜 `<!-- /@parts -->`（CSS は `/* @parts: 名前 */` 〜 `/* /@parts */`）のマーカーが付いています。

---

## 0. まず最初に差し替える「3点セット」

| 差し替えるもの | 場所 | 内容 |
| --- | --- | --- |
| **① 色** | `assets/css/style.css` の `:root`（`@parts: design-tokens`） | 配色・フォント・余白。ここを変えると全ページの見た目が一括で変わる |
| **② 文言** | 各 HTML（店名・住所・電話・営業時間・キャッチ・メニュー） | 後述の「一括置換リスト」を使うと速い |
| **③ 写真** | `assets/img/` ＋ 各 HTML の `.ph`（CSSダミー）を `<img>` に置換 | `撮影リスト.md` の構図に沿って撮影・差し替え |

### 文言の一括置換リスト（エディタの全ファイル置換で）

| 検索する文字列 | 意味 | 例 |
| --- | --- | --- |
| `炭火とビール 灯り屋` | 店名（フル） | 新店名 |
| `灯`（brand__mark / favicon） | ロゴの一文字 | 新店の一文字 |
| `055-000-0000` | 電話番号（tel:リンク・表示の両方） | 実番号 |
| `山梨県甲府市中央1-2-3` | 住所（地図URLにも含む） | 実住所 |
| `17:00〜23:30`／`L.O.23:00`／`日曜` | 営業時間・LO・定休 | 実営業情報 |
| `今日、寄ってく？` | FVキャッチ | 新キャッチ |
| `甲府駅 徒歩7分` | 最寄り | 実アクセス |

> ⚠️ 電話番号と住所は **複数ページ・複数箇所**（ヘッダー無し／info-card／footer／固定バー／地図URL）に出現します。必ず「全ファイル置換」で。チェック方法は後述。

---

## 1. 部品一覧

各部品の「用途 / 差し替え箇所（色変数・文言・画像）/ 設置ページ」。

### `header`（ヘッダー）
- **用途**：全ページ共通のロゴ＋グローバルナビ。スマホはハンバーガー（`main.js`が開閉）。
- **差し替え**：`brand__mark`（一文字ロゴ）、`brand__name`（店名）、ナビ項目。現在ページのリンクに `aria-current="page"` を付ける。
- **色**：`--color-bg` / `--color-accent`（現在ページ下線）。
- **設置**：全6ページ。**同一HTMLを複製**しているので、ナビ項目を増減したら全ページに反映する。

### `footer`（フッター）
- **用途**：店舗情報の再掲＋サイトマップ＋**制作サンプル注記**。
- **差し替え**：店名・キャッチ・住所・電話・営業・定休。
- **固定**：`.footer-note` の「※ このサイトは 山梨HPかけこみ寺 の制作サンプルです（架空の店舗です）」は**サンプルである限り必ず残す**。実案件納品時はこの注記ブロックを削除する。
- **設置**：全6ページ（複製）。

### `mobile-callbar`（固定電話バー）★居酒屋サイト最重要UI
- **用途**：スマホのみ画面下に固定。`[電話する]`／`[地図を見る]` の2ボタン。
- **差し替え**：`tel:` 番号、地図URL。
- **色**：`--color-accent`（電話ボタン地）、`--color-bg-3`（地図ボタン地）。高さは `--mobilebar-h`。
- **挙動**：`@media (min-width:768px)` で非表示。本文は `body { padding-bottom }` でバーに隠れない。
- **設置**：全6ページ（複製）。

### `info-card`（営業情報カード）
- **用途**：営業時間・定休・電話・席数を一目で。FV直下と access に設置。
- **差し替え**：`info-row` の dt/dd（項目）、`info-card__tel`、`info-card__actions` のリンク。
- **色**：`--color-bg-2`（地）、`--color-accent`（項目名・電話ボタン）、`--color-danger`（定休バッジ `.badge-closed`）。
- **設置**：index / access。

### `recommend-grid`（おすすめ3品グリッド）
- **用途**：看板メニュー3品をカード表示。スマホ1列→768pxで3列。
- **差し替え**：`rec-card` を3つ。画像（`.ph`→`<img>`）、`rec-card__name`、`rec-card__price`、`rec-card__desc`。
- **色**：`--color-accent`（価格）。
- **設置**：index。

### `menu-category`（メニューカテゴリ表）
- **用途**：品名＋価格の2カラムリスト。カテゴリ単位で繰り返す。
- **差し替え**：`menu-cat__title`（カテゴリ名）、`menu-item` を増減（品名・`.note` 補足・価格）。
- **色**：`--color-accent`（カテゴリ名）、`--color-accent-2`（価格）。
- **設置**：menu（フード3カテゴリ＋ドリンク3カテゴリ）。
- **関連**：`本日のタップ`（`.tap-board`）はクラフトビール専用の独立更新枠。日替わりで `tap-item` と `tap-board__date` だけ書き換える運用を想定。

### `access-section`（アクセス）
- **用途**：地図iframe＋住所カード＋道順ステップ＋駐車場。
- **差し替え**：`map-embed` の iframe `src`（後述）、`route-step` の写真とテキスト、`parking` の各施設。
- **色**：`--color-accent`（ステップ番号 `.route-step__no`）。
- **設置**：access（フル）／index（ダイジェスト＝地図＋住所カードのみ）。

### `news-list`（お知らせリスト）
- **用途**：日付・タグ・本文の3カラム行。新着を上に足すだけ。
- **差し替え**：`news-item` を追加（`__date` / `__tag` / `__text`）。
- **設置**：index。

### `head-common`（メタ情報）
- **用途**：title / description / OGP / favicon / Google Fonts / CSS 読み込み。
- **差し替え**：ページ複製時は `<title>`・`description`・`og:title`・`og:description`・`og:url` を必ず変更。
- **設置**：全6ページ。

### `design-tokens`（CSS変数）※`style.css`
- **用途**：配色・フォント・余白・レイアウトの一元管理。
- **差し替え**：ここだけで配色テーマが切り替わる。木目・暖色を別系統にすれば和食/洋風bar等へも転用可。

---

## 2. 地図iframe の差し替え

現在は甲府駅周辺を仮表示：

```html
<iframe ... src="https://www.google.com/maps?q=%E7%94%B2%E5%BA%9C%E9%A7%85&output=embed" ...></iframe>
```

実店舗版にするには Google マップで店舗を表示 → 「共有」→「地図を埋め込む」で得た iframe の `src` に差し替えます。`q=` を実住所のURLエンコード文字列に変えるだけでも可。
ボタン側のリンク（`maps/search/?api=1&query=住所`／`maps/dir/?api=1&destination=住所`）は住所文字列を置換すれば動きます。

---

## 3. プレースホルダ画像 `.ph` の差し替え

CSSダミー（グラデーション＋構図ラベル）。撮影後は以下のように `<img>` へ：

```html
<!-- before -->
<div class="ph ph--warm"><span class="ph__label"><strong>串の盛り</strong>盛り合わせ俯瞰</span></div>
<!-- after -->
<img src="assets/img/kushi-mori.jpg" alt="甲州地どりの串の盛り合わせ" loading="lazy">
```

`.ph` のバリエーション：`.ph`（木目）/ `.ph--warm`（暖色・炭火向き）/ `.ph--dark`（暗め・夜の外観向き）。必要枚数と構図は **撮影リスト.md** 参照。

---

## 4. 差し替え後のセルフチェック（コマンド例）

電話番号・住所の置換漏れ確認（Git Bash 等）：

```bash
# 旧番号・旧住所が残っていないか
grep -rn "055-000-0000" *.html
grep -rn "甲府市中央1-2-3" *.html
# サンプル注記の有無（実案件では 0 件になるべき）
grep -rln "山梨HPかけこみ寺" *.html
# 内部リンク切れ・tel:本数は README の「公開前チェック」参照
```
